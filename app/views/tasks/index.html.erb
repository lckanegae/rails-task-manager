<h1>Index of Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to "#{task.title} - Details: #{task.details} - Completed: #{task.completed}", task_path(task) %>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to 'Add Task', new_task_path %>
</p>
